//
// Created by yaojq on 2024/1/17.
//

#include "../include/Runtime.h"

int main() {
    int x;
    DSE_Input(x);
    int y;
    DSE_Input(y);
    int z;
    DSE_Input(z);
    if (x ==1 &&z==x) {
        if (y == 1024) {
            int z = 4 / (y - 1024);
        }
    }
    return 0;
}
